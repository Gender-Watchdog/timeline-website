# Copilot Instructions

- Always use the `dw-env` conda environment for Python operations.
- When running Python scripts, use `conda run -n dw-env python <script_name>` or ensure the terminal has the environment activated.
- For installing packages, use `conda install -n dw-env <package>` or `pip` within the activated environment.
